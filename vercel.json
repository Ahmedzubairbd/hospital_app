{
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["sin1"],
  "env": {
    "NEXT_PUBLIC_API_URL": "https://hospital-app-drab.vercel.app",
    "DATABASE_URL": "${DATABASE_URL}"
  },
  "build": {
    "env": {
      "NEXT_PUBLIC_API_URL": "https://api.hospital-app-drab.vercel.app",
      "DATABASE_URL": "${DATABASE_URL}"
    }
  }
}
